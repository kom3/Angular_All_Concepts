import { MyfirstdirectiveDirective } from './myfirstdirective.directive';

describe('MyfirstdirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new MyfirstdirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
