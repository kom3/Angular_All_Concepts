import { MyfirstpipPipe } from './myfirstpip.pipe';

describe('MyfirstpipPipe', () => {
  it('create an instance', () => {
    const pipe = new MyfirstpipPipe();
    expect(pipe).toBeTruthy();
  });
});
